name: Dependencies

on: [workflow_dispatch]

jobs:
  dependencies:
    name: Update Dependencies
    uses: Katsute/Workflows/.github/workflows/npm.run.yml@main
    with:
      command: npm i
      commit-files: "'dependencies/node'"
      commit-message: Update dependencies
